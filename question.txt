//理解
構造体のメンバ変数であるint noMembersの初期値は0である。
is_Member_Of関数内のfor文についてSetAddの最初の要素は必ず配列にappendされる。

//疑問
・scanningPositionとnoMembersの関係性
　・noMembersは最終的には配列の要素数
　・scanningPositionは配列の読み取り位置のインデックス

★質問したいところ
　・s1,s2はacc_set型
　・和集合、積集合、差集合の配列はset型を使用する
　　出力するだけで、操作をしないからか？？
    　・SetUnion
    　・SetIntersection
    　・SetDifference
　・上記の３つの関数ではset型のsolSetを使用している。
　　acc_setではない理由は何ですか？？
　　SetAdd関数の引数はacc_set型だから、
　　acc_set関数を使用するべきだと思った。

★コード内の不備？？
・HPAlloc関数でヒープ領域を確保したが、
　確保されたメモリの解放処理を行っていない気がする。
